name: polypick_enterprise
description: World's Most Advanced AI Dialer & Operations Suite for Raju Singh.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_tts: ^3.8.5
  speech_to_text: ^6.3.0
  geolocator: ^10.1.0
  permission_handler: ^11.1.0
  record: ^5.1.2
  url_launcher: ^6.2.2
  path_provider: ^2.1.1
  http: ^1.2.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.8
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/polypick_original_knowledge.json
```[cite: 1]


name: polypick_enterprise
description: World's Most Advance AI Smart Dialer
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_generative_ai: ^0.4.6
  record: ^5.0.4
  permission_handler: ^11.3.0
  geolocator: ^11.0.0
  url_launcher: ^6.2.4
  flutter_tts: ^4.0.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  intl: ^0.19.0
  contacts_service: ^0.6.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true